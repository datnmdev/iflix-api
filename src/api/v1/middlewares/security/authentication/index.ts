import authenticateAccessToken from './accessToken'
import authenticateRefreshToken from './refreshToken'

const authentication = {
  authenticateAccessToken,
  authenticateRefreshToken
}

export default authentication